# FastMCP and MCP SDK with version pins to avoid task group bug
fastmcp==2.13.0.2
mcp>=1.18.0

# CRITICAL: These specific versions fix "Task group is not initialized" error
# Do not upgrade without testing!
starlette==0.49.1
anyio==4.6.2.post1

# Server
uvicorn[standard]==0.38.0

# HTTP client (for MCP client testing)
httpx>=0.27.0

# Redis for future task queue
redis>=5.0.0
