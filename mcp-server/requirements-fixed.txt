# Known-good version combo for FastMCP (Option A - minimal changes)
# Fixes "Task group is not initialized" issue

# Core MCP & FastMCP
fastmcp==2.13.0.2
mcp>=1.18.0

# Critical version pins to avoid task group bug
starlette==0.49.1
anyio==4.6.2.post1

# Server & transports
uvicorn==0.38.0
sse-starlette>=2.0.0

# HTTP client (for MCP client)
httpx>=0.27.0
httpx-sse>=0.4.0
